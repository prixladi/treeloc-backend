version: '3.4'

services:

  api:
    image: treeloc/api
    ports:
      - "4545:80"
    environment:
      MONGO_URL: "mongodb://mongodb:27017"
      MONGO_DATABASE_NAME: "TreeLocApi"
    build:
      context: .
      dockerfile: src/TreeLoc.Api/Dockerfile

  mongodb:
    image: mongo:latest
    environment:
      MONGO_DATA_DIR: "/data/db"
      MONGO_LOG_DIR: "/dev/null"
    volumes:
      - mongodata:/data/db
    ports:
      - 27017:27017

volumes:
  mongodata: